'use strict'

const assert = require('chai').assert
const path = require('path')
const insertTestData = require(path(__dirname, '../src/core/insert-test-data'))

describe('insert test data tests', function() {
  it('insert client data', function(done) {
    
    done()
  })
}
